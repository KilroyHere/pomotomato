/**
 * USAGE INSTRUCTIONS:
 * 
 * 1. Copy this file to env.js in the same directory
 * 2. Replace "YOUR_SPOTIFY_CLIENT_ID_HERE" with your actual Spotify Client ID
 * 3. Make sure env.js is in your .gitignore to avoid committing your client ID
 */

window._env_ = {
  SPOTIFY_CLIENT_ID: "YOUR_SPOTIFY_CLIENT_ID_HERE"
} 